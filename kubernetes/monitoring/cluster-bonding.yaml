apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRoleBinding
metadata:
  name: prometheus
roleRef:
  apiGroup: rbac.authorization.k8s.io
  kind: ClusterRole
  name: prometheus           # the role defined above
subjects:
  - kind: ServiceAccount
    name: prometheus         # the service account above
    namespace: monitoring